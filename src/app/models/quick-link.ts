export default class QuickLinkModel {

    constructor(title, descr, url) {
        this.title = title
        this.descr = descr
        this.url=url
    }

    title : string
    descr: string
    url: string
}